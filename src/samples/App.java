package samples;

public class App {
    public static void main(String[] args) throws Exception {
        // Encapsulation
        // Vehicle v1 = new Vehicle();
        // v1.setColor("Red");
        // System.out.println(v1.getColor());

        // Inheritance
        // Dog d = new Dog();
        // d.eat();

        // Polymorphism
        // Runtime Polymorphism or Method Overriding
        // Cat a1 = new Cat();
        // a1.eat();

        // Compile time Polymorphism or Method Overloading
        System.out.println(Program.max(5, 10));
        System.out.println(Program.max(20.20, 40.45));
    }
}
